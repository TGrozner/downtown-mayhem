# Draco decoder

Local copy of the Three.js Draco glTF decoder files from:
`node_modules/three/examples/jsm/libs/draco/gltf/`

These files let `DRACOLoader` decode compressed Quaternius glTF/GLB assets without
depending on an external CDN at runtime.
